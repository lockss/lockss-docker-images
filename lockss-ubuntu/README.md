# LOCKSS Ubuntu Docker Image

This Dockerfile defines an Ubuntu image with an IPM implementation, and a few essential packages pre-installed, to be used as a base image for other LOCKSS-related projects.

The only available tag at this time is `xenial`, corresponding to `FROM ubuntu:xenial`.
